# SSM 프로젝트 요구사양

## 1. 최상위 탭 기반 레이아웃 (Tab-based Architecture)

### 1.1 개요
사용자의 작업 환경을 관심사에 따라 분리하여 효율적인 파라미터 관리 경험을 제공합니다.

### 1.2 구성
- **🖥️ UI 편집 (UI Edit)**: YAML 편집기와 실시간 동적 UI 프리뷰를 제공하는 메인 작업 공간
- **🧮 수식 편집 (Formula Edit)**: 자유로운 수식 작성, 평가 및 새로운 결과 변수 등록을 위한 전문 편집기
- **📋 데이터 레지스트리 (Data Registry)**: 등록된 모든 파라미터의 현재 값과 수식 로직을 한눈에 관리하는 통합 대시보드

---

## 2. 수식 편집기 (Formula Editor)

### 2.1 개요
YAML에 정의된 파라미터를 활용한 사칙연산 수식을 작성하고, 평가된 결과를 새로운 변수로 등록하는 기능을 제공합니다.

### 2.2 주요 기능
- **수식 작성 및 평가**: 사칙연산, 괄호, 변수명을 조합한 수식 지원 및 실시간 결과 산출
- **변수 자동완성**: VS Code 스타일의 자동완성(IntelliSense) 지원, 타입별 아이콘 및 현재값 미리보기 제공
- **LaTeX 프리뷰**: 작성 중인 수식을 고품질 수학 기호 형식(KaTeX)으로 실시간 렌더링
- **출력 변수 등록**: 계산된 결과와 적용된 수식 원본을 사용자 지정 이름으로 시스템에 등록

### 2.3 기술 사양
- **Context Aware**: 타임 분리 정책에 따라 YAML 정의 변수와 기계산된 변수 모두 참조 가능
- **Error Handling**: 미등록 변수 참조 및 구문 오류 실시간 트래킹

---

## 3. 데이터 레지스트리 (Parameter Registry)

### 3.1 개요
시스템 내 가용한 모든 파라미터를 리스트 기반의 테이블 레이아웃으로 시각화하여 데이터의 가시성과 추적성(Traceability)을 보장합니다.

### 3.2 상세 요구사항
- **리스트 기반 레이아웃**: 변수명, 값/수식, 타입, 출처, 설명을 포함하는 테이블 형식
- **인라인 수식 시각화**: 
  - 수식 편집기 등록 변수 및 YAML 내 수식 정의(`(A+B)` 형태) 자동 감지
  - `(수식) = 결과값 + 단위` 형태의 인라인 결합 뷰 제공
- **출처 관리(Source Tracking)**:
  - **YAML**: 설정 파일에 직접 정의된 파라미터
  - **CALCULATED**: 수식 편집기를 통해 동적으로 생성된 파라미터
- **타입 및 제약 조건 표시**: 각 파라미터의 데이터 타입 및 설정된 Range/Options 정보를 배지 형태로 표시

---

## 4. 동적 UI 생성기 (Dynamic UI Generator)

### 4.1 개요
YAML 정의에 따라 입력 필드를 동적으로 생성하고, 변수 간의 논리적 종속성을 실시간으로 반영합니다.

### 4.2 주요 기능
- **다양한 타입 지원**: Double, Integer, Boolean, String, Group 등
- **조건부 렌더링 (Condition)**: 특정 변수의 상태에 따른 UI 구성 요소 노출 제어
- **실시간 데이터 바인딩**: YAML 값 변경 시 UI 상태 즉시 업데이트 및 수식 재평가

---

## 5. 향후 확장 계획
- 수학 함수 지원 (sin, cos, sqrt 등)
- 수식 히스토리 저장 및 복원
- 복잡한 논리식 지원 (if/then/else)
- 파라미터 변경 이력 트래킹 (Change Logs)
